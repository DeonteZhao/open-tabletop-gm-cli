- [x] 项目基础结构建立完毕，`requirements.txt` 仅依赖必须库。
- [x] 执行 `python main.py config` 可以顺利完成交互式配置，配置文件存储路径与权限（600）正确。
- [x] 未配置时执行 `python main.py load` 会正确提示进行配置。
- [x] 环境变量配置能正确覆盖文件配置。
- [x] `python main.py new <name>` 能够正确复制模板并在指定路径建立存档。
- [x] `python main.py list` 能正确列出已有战役。
- [x] `python main.py load <name>` 能成功与 LLM 建立连接并输出中文开场。
- [x] 在游戏中输入自然语言描述，LLM 能正确判断并调用相关脚本（如 `dice.py`），且结果翻译为中文呈现给玩家。
- [x] 基础斜杠命令（`/save`, `/help`, `/recap`, `/end` 等）功能运行正常。
- [x] PDF 模组导入（命令行及斜杠命令方式）能正确解析文本并分块调用 LLM，更新世界状态文件。
- [x] API 异常和失败情况能够被妥善捕获并提示，不引发崩溃。
